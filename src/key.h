/*
 * 文件名：key.h
 * 功能：按键扫描头文件
 * 描述：定义按键引脚和按键扫描函数声明
 * 作者：[作者名]
 * 日期：2025/08/27
 */

#ifndef __KEY_H__
#define __KEY_H__

#include "reg52.h"      // 52单片机寄存器定义

// 按键引脚定义（低电平有效）
sbit KEY1 = P2^2;       // 按键1：测温功能
sbit KEY2 = P2^1;       // 按键2：调高阈值温度
sbit KEY3 = P2^0;       // 按键3：调低阈值温度

/*
 * 函数名：Key_Scan
 * 功能：扫描按键状态
 * 参数：无
 * 返回值：按键值（1-4表示对应按键按下，0表示无按键按下）
 */
unsigned char Key_Scan(void);

#endif